// build-info.typ
// This file is copied to .render-jpcv and can be overwritten by the CLI at build time.
#let render_date = (
  year: datetime.today().year(),
  month: datetime.today().month(),
  day: datetime.today().day(),
)
